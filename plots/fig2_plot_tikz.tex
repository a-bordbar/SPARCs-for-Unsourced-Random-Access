\documentclass[tikz,border=2pt]{standalone}

\usepackage{pgfplots}
\pgfplotsset{compat=1.18}

\begin{document}
\begin{tikzpicture}
\begin{axis}[
    width=11cm,
    height=7.2cm,
    xmode=log,
    ymode=log,
    log basis x=10,
    log basis y=10,
    xmin=1e-6,
    xmax=1e-1,
    ymin=1e-6,
    ymax=1,
    grid=both,
    minor tick num=9,
    major grid style={line width=0.25pt, draw=black!22},
    minor grid style={line width=0.15pt, draw=black!10},
    xlabel={$P_{\mathrm{FA}}$},
    ylabel={$P_{\mathrm{MD}}$},
    legend pos=south east,
    legend cell align=left,
    legend style={fill=white, draw=black},
]

\addplot[
    blue,
    thick,
    dashed,
] table[
    col sep=comma,
    x=p_fa,
    y=p_md,
    restrict expr to domain={\thisrow{snr_dB}}{9.999:10.001},
] {../csv_files/fig2_theoretical.csv};
\addlegendentry{Theoretical simulations}

\addplot[
    red!70!black,
    thick,
    dashed,
    forget plot,
] table[
    col sep=comma,
    x=p_fa,
    y=p_md,
    restrict expr to domain={\thisrow{snr_dB}}{12.999:13.001},
] {../csv_files/fig2_theoretical.csv};

\addplot[
    green!50!black,
    thick,
    dashed,
    forget plot,
] table[
    col sep=comma,
    x=p_fa,
    y=p_md,
    restrict expr to domain={\thisrow{snr_dB}}{14.999:15.001},
] {../csv_files/fig2_theoretical.csv};

\addplot[
    blue,
    thick,
    solid,
    no markers,
] table[
    col sep=comma,
    x=p_fa,
    y=p_md,
    restrict expr to domain={\thisrow{snr_dB}}{9.999:10.001},
] {../csv_files/fig2_monte_carlo.csv};
\addlegendentry{Monte-carlo simulations}

\addplot[
    red!70!black,
    thick,
    solid,
    no markers,
    forget plot,
] table[
    col sep=comma,
    x=p_fa,
    y=p_md,
    restrict expr to domain={\thisrow{snr_dB}}{12.999:13.001},
] {../csv_files/fig2_monte_carlo.csv};

\addplot[
    green!50!black,
    thick,
    solid,
    no markers,
    forget plot,
] table[
    col sep=comma,
    x=p_fa,
    y=p_md,
    restrict expr to domain={\thisrow{snr_dB}}{14.999:15.001},
] {../csv_files/fig2_monte_carlo.csv};

\draw[->, thick, blue]
    (axis cs:1.1e-2,5.5e-2)
    -- (axis cs:2.3e-2,1.2e-1)
    node[pos=0, below, black] {$10$ dB};

\draw[->, thick, red!70!black]
    (axis cs:1.3e-3,1.6e-2)
    -- (axis cs:3.0e-3,4.0e-2)
    node[pos=0, below, black] {$13$ dB};

\draw[->, thick, green!50!black]
    (axis cs:7.0e-4,1.8e-3)
    -- (axis cs:1.4e-3,4.5e-3)
    node[pos=0, below, black] {$15$ dB};

\end{axis}
\end{tikzpicture}
\end{document}
